# Test Environment Variables
NODE_ENV=test

# Database Configuration (use separate test database)
DB_TYPE=mssql
DB_HOST=localhost
DB_PORT=1433
DB_USERNAME=sa
DB_PASSWORD=YourTestPassword123!
DB_DATABASE=EventTicketingTest
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRATION=24h

# Server Configuration
PORT=3001

# Image Storage
IMAGE_STORAGE_PATH=./test-uploads
